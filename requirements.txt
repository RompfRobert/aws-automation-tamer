boto3==1.40
tabulate==0.9.0
# Optional: for systemd journal logging on Linux
systemd-python; sys_platform == "linux"

# Development and testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
moto>=4.2.0  # AWS service mocking